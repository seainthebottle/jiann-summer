# 일시 정지 기능 구현 마스터 플랜

## 1. 목적

공부 타이머에 일시 정지와 재개 기능을 추가한다. 일시 정지 전후의 실제 공부 시간은 서로 다른 시간 구간으로 저장하지만, 사용자가 처음 `시작`한 뒤 최종적으로 `종료`할 때까지는 하나의 공부 실행이자 공부 횟수 1회로 취급한다.

이 문서는 해당 기능의 설계와 구현 순서를 정하는 기준 문서다. 구현 중 세부 사항이 달라질 경우 코드만 임의로 변경하지 않고 이 문서의 결정 사항과 작업 현황을 함께 갱신한다.

## 2. 핵심 요구사항

- 대기 상태에서는 지금처럼 하나의 `시작` 버튼을 표시한다.
- 시작 후에는 기존 종료 버튼 앞에 `일시 정지` 버튼을 표시한다.
- 일시 정지 상태에서는 `일시 정지` 버튼이 `재개` 버튼으로 바뀐다.
- 일시 정지 상태에서도 사용자는 공부 실행을 최종 종료할 수 있다.
- 일시 정지 중에는 타이머와 공부 시간이 증가하지 않는다.
- 재개하면 일시 정지 전까지의 누적 시간부터 타이머가 이어진다.
- 일시 정지 전후의 공부 구간은 데이터와 일간 차트에서 분리한다.
- 여러 구간으로 분리되더라도 공부 횟수는 최종 종료 전까지 1회로 계산한다.
- 일반 과목 공부와 계획에서 시작한 공부에 동일한 동작을 적용한다.
- 실행 중 또는 일시 정지 중 새로고침해도 상태와 누적 시간이 복원되어야 한다.

## 3. 용어와 집계 원칙

### 공부 실행

사용자가 `시작`을 누른 시점부터 `종료`를 누른 시점까지의 논리적인 한 번의 공부다. 중간에 일시 정지를 여러 번 하더라도 같은 공부 실행이다.

### 공부 구간

실제로 시간이 흐른 연속 구간이다. 일시 정지를 누르면 현재 구간을 닫고, 재개하면 같은 공부 실행에 속하는 새 구간을 연다.

예시:

```text
10:00 시작 ── 10:20 일시 정지   10:30 재개 ── 10:50 종료
             구간 1: 20분                    구간 2: 20분

실제 공부 시간: 40분
정지 시간: 10분(공부 시간에서 제외)
공부 구간 수: 2개
공부 횟수: 1회
```

### 집계 기준

- 공부 시간: 각 공부 구간의 실제 지속 시간 합계
- 공부 횟수: 동일한 공부 실행 식별자의 고유 개수
- 계획 진행 시간: 해당 계획에 연결된 공부 구간의 실제 지속 시간 합계
- 일간 차트: 공부 구간별로 표시하며 정지 시간은 빈 시간으로 표시
- 날짜 범위 집계: 각 공부 구간과 조회 날짜 범위가 겹치는 시간만 계산

## 4. 상태 모델

타이머의 클라이언트와 서버 상태를 다음 세 가지로 통일한다.

| 상태 | 열린 공부 구간 | 타이머 증가 | 주 동작 |
| --- | --- | --- | --- |
| `idle` | 없음 | 아니요 | 시작 |
| `running` | 있음 | 예 | 일시 정지 또는 종료 |
| `paused` | 없음 | 아니요 | 재개 또는 종료 |

상태 전환은 다음 규칙을 따른다.

```text
idle --시작--> running
running --일시 정지--> paused
paused --재개--> running
running --종료--> idle
paused --종료--> idle
```

- 중복 시작, 중복 일시 정지, 실행 중 재개 같은 잘못된 전환은 서버에서 거부한다.
- 네트워크 재시도에 의해 시간이 중복 반영되지 않도록 상태 확인과 변경을 원자적으로 처리한다.
- 클라이언트의 버튼 클래스만을 진실의 원천으로 사용하지 않고 서버 상태를 기준으로 복원한다.

## 5. 데이터 모델 및 마이그레이션

### 기본 방향

현재 `study_sessions`의 한 행은 하나의 공부 구간으로 유지한다. 여기에 여러 구간을 하나의 공부 실행으로 묶는 식별자를 추가한다.

구현 시 다음 구조를 우선안으로 사용한다.

- `study_sessions.session_group_id`: 동일한 공부 실행의 모든 구간이 공유하는 식별자
- 일시 정지 시 현재 행의 `end_time`과 `duration_seconds` 확정
- 재개 시 같은 `session_group_id`, `user_id`, `subject_id`, `plan_id`를 가진 새 행 생성
- 최종 종료 여부와 일시 정지 상태를 복원할 수 있는 실행 수준 상태 저장

실행 수준 상태는 별도 실행 테이블에 저장한다.

- 테이블: `study_session_groups`
- 필드: `id`, `user_id`, `subject_id`, `plan_id`, `status`, `started_at`, `ended_at`, `created_at`
- `status`: `running`, `paused`, `ended`

별도 실행 테이블을 사용하면 일시 정지 중 열린 `study_sessions` 행이 없어도 재개 대상과 상태를 명확히 찾을 수 있고, 공부 횟수를 안정적으로 집계할 수 있다. 과거 기록은 `session_group_id`가 `NULL`인 각 행을 독립된 한 번의 공부로 취급한다.

### 기존 데이터 호환

- 기존 `study_sessions` 각 행은 독립된 공부 실행으로 간주한다.
- 기존 통계 시간은 변경되지 않아야 한다.
- 기존 행에 그룹 식별자를 채울 때 서로 다른 과거 세션이 합쳐지지 않도록 각 행에 고유 그룹을 배정한다.
- 마이그레이션은 여러 번 실행해도 결과가 같은 멱등 방식으로 작성한다.
- `database/schema.sql`과 `server/server.js`의 자동 마이그레이션을 함께 갱신한다.
- 마이그레이션 실패 시 서버가 부분 적용 상태를 숨기지 않도록 오류를 명확히 기록한다.

## 6. 서버 API 계획

### 시작

- 현재 사용자의 진행 중이거나 일시 정지된 공부 실행이 없는지 확인한다.
- 새 공부 실행과 첫 공부 구간을 생성한다.
- 계획 공부라면 해당 계획을 진행 상태로 변경한다.
- 생성된 실행 식별자와 서버 기준 시작 시각을 응답한다.

### 일시 정지

- 현재 사용자의 `running` 공부 실행과 열린 공부 구간을 찾는다.
- 서버 시각을 기준으로 구간을 닫고 지속 시간을 확정한다.
- 실행 상태를 `paused`로 변경한다.
- 계획 공부라면 확정된 구간 시간을 계획 진행 시간에 한 번만 반영한다.
- 누적 실제 공부 시간을 응답한다.

### 재개

- 현재 사용자의 `paused` 공부 실행을 찾는다.
- 같은 실행 식별자와 과목·계획 정보를 가진 새 공부 구간을 생성한다.
- 실행 상태를 `running`으로 변경한다.
- 새 구간의 서버 기준 시작 시각과 이전 누적 시간을 응답한다.

### 종료

- `running` 상태라면 마지막 열린 구간을 먼저 닫는다.
- `paused` 상태라면 새 시간 구간을 만들지 않고 실행만 종료한다.
- 실행 상태와 종료 시각을 확정한다.
- 계획 누적 시간과 목표 시간을 비교해 계획 상태를 재계산한다.
- 통계와 UI 갱신에 필요한 최종 누적 시간을 응답한다.

### 상태 조회

다음 정보를 반환하도록 확장한다.

- 상태: `idle`, `running`, `paused`
- 공부 실행 식별자
- 과목 ID와 과목명
- 계획 ID와 계획명
- 이전에 확정된 누적 공부 시간
- 현재 구간 시작 시각(`running`일 때만)
- 전체 실행 시작 시각

### 라우트 및 클라이언트 API

- 기존 시작·종료 API의 호환성을 최대한 유지한다.
- 일시 정지와 재개 라우트를 추가한다.
- `js/api.js`에 대응 메서드를 추가한다.
- 인증된 사용자가 자신의 실행, 과목, 계획만 변경할 수 있도록 모든 변경 API에서 소유권을 확인한다.

## 7. 계획 상태 및 누적 시간 규칙

- 계획 공부 시작: 계획 상태를 `in_progress`로 변경
- 계획 공부 일시 정지: 재개 가능한 상태임을 표현하되 완료로 처리하지 않음
- 계획 공부 재개: `in_progress`로 복원
- 계획 공부 종료: 누적 시간이 목표 이상이면 `done`, 아니면 `todo`
- 수동 완료와 완료 취소 기능은 기존 동작을 유지하되 활성 또는 일시 정지된 실행과 충돌하지 않아야 함
- 계획의 `completed_seconds`에는 닫힌 공부 구간을 정확히 한 번만 반영
- 일시 정지와 종료 요청이 반복되어도 같은 구간 시간이 중복 누적되지 않아야 함

계획 상태에 `paused`를 추가할지, 실행 상태만 `paused`로 두고 계획은 별도 표시할지는 구현 시 기존 카드 필터와 수정 제한 로직을 함께 검토해 결정한다. 어떤 방식을 택하더라도 UI에서는 해당 계획이 일시 정지되어 재개 가능함을 분명히 표시한다.

## 8. 화면 및 타이머 구현

### 메인 공부 컨트롤

- `index.html`의 공부 버튼 영역을 여러 버튼을 담을 수 있는 컨테이너로 변경한다.
- `idle`: 시작 버튼 하나를 전체 너비로 표시
- `running`: 왼쪽에 일시 정지, 오른쪽에 종료 표시
- `paused`: 왼쪽에 재개, 오른쪽에 종료 표시
- 작은 화면에서도 버튼 순서가 유지되고 누르기 쉬운 크기를 확보한다.
- 과목 선택은 `running`과 `paused` 모두 비활성화한다.

### 타이머 계산

화면 표시 시간은 다음과 같이 계산한다.

```text
표시 시간 = 서버가 확정한 이전 구간 누적 시간
          + 현재 시각 - 현재 구간 시작 시각(running일 때만)
```

- `paused`에서는 이전 구간 누적 시간만 표시한다.
- 클라이언트 시계 오차를 줄이기 위해 서버가 반환한 시각을 기준으로 초기화한다.
- 기존 오늘 총 공부, 선택 과목 공부, 계획 카드 실시간 진행률도 같은 표시 시간을 사용한다.
- `stop()`이 무조건 `00:00:00`으로 초기화하는 현재 동작은 최종 종료에서만 실행되도록 상태별 메서드로 분리한다.

### 계획 카드

- 실행 중인 계획에는 `일시 정지`와 `계획 중지` 동작을 표시한다.
- 일시 정지된 계획에는 `재개`와 `계획 종료` 동작을 표시한다.
- 계획 카드의 진행 시간은 확정 누적 시간과 현재 구간 시간을 합산한다.
- 정지 중에는 진행률과 남은 시간이 증가하거나 감소하지 않는다.

## 9. 통계 및 차트 변경

- 시간 합계 쿼리는 공부 구간 단위 계산을 유지한다.
- 공부 횟수는 세션 행 수가 아니라 고유 공부 실행 수로 계산한다.
- `sessionCount`처럼 세션 배열 길이를 사용하는 항목은 의미를 확인하고 논리 실행 수와 구간 수를 구분한다.
- 필요한 경우 API 응답에 다음을 별도로 제공한다.
  - `studyCount`: 공부 실행 횟수
  - `segmentCount`: 실제 공부 구간 수
- 일간 원형 차트는 구간 사이의 정지 시간을 빈 구간으로 표시한다.
- 활성 강조는 현재 열린 구간에만 적용하고, 일시 정지된 실행은 활성 시간으로 계속 늘리지 않는다.
- 일간·주간·월간 총합과 과목별 평균에서 정지 시간을 제외한다.

## 10. 짧은 공부 기록 규칙

현재 서버는 종료된 한 세션이 1분 이하이면 해당 행을 삭제한다. 일시 정지로 구간이 나뉘면 각 구간별 삭제는 정상적인 전체 공부 시간을 잃게 만들 수 있으므로 다음 기준으로 변경한다.

- 1분 이하 판단은 개별 공부 구간이 아니라 최종 공부 실행의 누적 실제 공부 시간을 기준으로 한다.
- 최종 누적 시간이 기준 이하라면 해당 실행의 모든 구간을 기록에서 제외한다.
- 누적 시간이 기준을 넘으면 1분 이하의 짧은 개별 구간도 전체 실행의 일부로 보존한다.
- 일시 정지 시점에는 짧다는 이유로 구간을 삭제하지 않는다.
- 기존 정책의 `1분 이하`가 정확히 60초를 포함하는 현재 동작은 유지한다.

## 11. 동시성 및 오류 처리

- 상태 변경 작업은 가능하면 DB 트랜잭션으로 묶는다.
- 한 사용자에게 동시에 둘 이상의 미종료 공부 실행이 생기지 않도록 한다.
- 일시 정지와 종료 요청이 거의 동시에 도착해도 하나만 유효하게 처리한다.
- API 실패 시 클라이언트가 먼저 상태를 바꾸지 않고 서버 성공 후 UI를 전환한다.
- 응답을 잃은 경우 상태 조회를 통해 실제 서버 상태로 복구할 수 있게 한다.
- 오래된 서비스 워커 캐시로 HTML과 JS 버전이 불일치하지 않도록 캐시 버전을 갱신한다.

## 12. 구현 순서

- [x] 현재 타이머, 계획, 통계의 세션 처리 흐름 확인
- [x] 요구사항과 상태 모델을 마스터 플랜으로 문서화
- [x] 실행 그룹 데이터 모델 확정
- [x] 스키마와 자동 마이그레이션 구현
- [x] 시작·일시 정지·재개·종료 서버 로직 구현
- [x] 상태 조회 및 통계 집계 로직 수정
- [x] 클라이언트 API 메서드 추가
- [x] 메인 타이머 버튼과 상태별 UI 구현
- [x] 계획 카드의 일시 정지·재개 UI 구현
- [x] 새로고침 상태 복원 구현
- [x] 서비스 워커 캐시 버전 갱신
- [x] 서버 정적 검사
- [ ] 실제 DB를 사용한 수동 API 시나리오 검증
- [ ] 브라우저 UI 및 회귀 시나리오 검증
- [ ] 구현 결과에 맞춰 본 문서 체크리스트와 결정 사항 갱신

## 13. 검증 시나리오

### 기본 동작

- 일반 공부: 시작 → 종료
- 일반 공부: 시작 → 일시 정지 → 재개 → 종료
- 계획 공부: 시작 → 일시 정지 → 재개 → 종료
- 한 실행에서 여러 번 일시 정지와 재개
- 일시 정지 상태에서 바로 종료

### 시간과 횟수

- 정지 중 타이머가 증가하지 않음
- 재개 후 이전 누적 시간부터 이어짐
- 두 구간의 실제 시간 합계가 오늘·주간·월간 통계에 반영됨
- 두 구간 사이 정지 시간이 통계에서 제외됨
- 구간이 여러 개여도 공부 횟수는 1회
- 별도로 종료하고 다시 시작하면 공부 횟수가 2회

### 계획

- 일시 정지 시 계획 진행 시간이 보존됨
- 재개 후 동일 계획에 시간이 이어서 누적됨
- 최종 종료 시 목표 달성 여부에 따라 상태가 정확히 결정됨
- 활성 또는 일시 정지된 계획의 수정·삭제 제한이 일관됨

### 복원과 경계 조건

- 실행 중 새로고침
- 일시 정지 중 새로고침
- 실행 중 로그아웃 후 재로그인
- 자정 전 시작 후 자정 이후 일시 정지 또는 종료
- 총 공부 시간이 60초 이하인 실행
- 짧은 구간 여러 개의 합이 60초를 초과하는 실행
- 일시 정지·재개·종료 버튼의 빠른 중복 클릭
- API 오류와 네트워크 단절 후 상태 재조회

### 회귀 검증

- 기존 과목 선택과 일반 시작·종료
- 계획 생성·수정·완료·완료 취소·삭제
- 오늘 총 공부와 선택 과목 공부 실시간 표시
- 일간 차트 및 일간·주간·월간 통계
- 관리자 사용자별 통계 조회
- 모바일 화면과 PWA 재실행

## 14. 완료 조건

다음 조건을 모두 충족해야 구현이 완료된 것으로 본다.

- 메인 타이머와 계획 타이머에서 일시 정지·재개가 동작한다.
- 실제 공부 구간과 정지 구간이 정확히 분리된다.
- 여러 공부 구간이 하나의 공부 횟수로 집계된다.
- 계획 누적 시간이 중복되거나 유실되지 않는다.
- 실행 중과 일시 정지 중 새로고침 복원이 가능하다.
- 기존 데이터와 기존 시작·종료 기록의 통계가 유지된다.
- 정의된 검증 시나리오를 통과한다.
- 구현 결과와 남은 제약 사항이 이 문서에 반영되어 있다.
